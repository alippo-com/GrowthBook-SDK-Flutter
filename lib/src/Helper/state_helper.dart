/// It will be helpful incase we want to show status of our sdk
/// builder.

enum StateHelper {
  /// While fetching.
  loading,

  /// While fetched.
  fetched,

  /// While error ocurred.
  error
}
